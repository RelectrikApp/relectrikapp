# Copy to .env.local and fill in your real values.
# Get the connection string from: Supabase Dashboard → Project Settings → Database → Connection string (URI)

# Database (Supabase PostgreSQL)
# Replace REGION with your actual region, e.g. us-east-1, eu-west-1 (see Supabase dashboard)
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres"

# NextAuth (for production you'll set a secret)
# NEXTAUTH_SECRET=your-secret-here
# NEXTAUTH_URL=http://localhost:3000

# Google Maps (dashboard: ver técnicos en vivo en /dashboard/map)
# Obtén la API key en: https://console.cloud.google.com/apis/credentials
# Activa "Maps JavaScript API" para el mapa de técnicos.
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
